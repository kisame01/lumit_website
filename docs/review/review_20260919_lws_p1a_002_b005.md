# Review — LWS-P1A-002, B-005: the shared chrome must appear exactly once per page

**Verdict: APPROVE.** Reviewed 2026-09-19 by the Opus 5 planner context that opened LWS-P1-003.

**Nothing here is taken from the implementer's report.** The tree was staged into the reviewer's
container and `node scripts/check_site.mjs` was run there. All four contract mutations were
reproduced independently, and two further mutations the contract did not ask for were run as well.
Container Node is v22.22.2; the implementer ran v24.14.1. Nothing in the checker is version-sensitive.

---

## 1. Scope — one file, and the proof

| file | before | after |
|---|---|---|
| `scripts/check_site.mjs` | 8,016 | **9,413** |

Every other file is byte-identical to the LWS-P1A-001 baseline. `package.json` (132), `.gitignore`
(46) and `.gitattributes` (902) match on **size and mtime both** — they were not opened. The eleven
site files match §6 exactly. `about/`, `engagements/` and `how-we-work/` carry moved mtimes and
unchanged sizes, which is mutate-and-restore and is what the contract asked for.

No CRLF anywhere. `.gitattributes` held. RULE 004 intact.

## 2. Clean run — reproduced, not accepted

```
site check: 6 pages | 68 internal links | 6 assets
site check ok
```

Exit 0. Banner matches §4. Every page carries exactly one masthead, one closing block and one footer.

## 3. The four mutations — all four reproduced exactly

| # | red | exit | restored |
|---|---|---|---|
| M1 second, different `<footer>` in `about/` | `about/index.html:100  B-005 footer occurs 2 times, expected exactly 1` | 1 | 6,342 |
| M2 duplicated closing block in `engagements/` | `engagements/index.html:92  B-005 closing block occurs 2 times, expected exactly 1` | 1 | 5,788 |
| M3 `<footer>` deleted from `how-we-work/` | `how-we-work/index.html:1  B-005 footer occurs 0 times, expected exactly 1` | 1 | 6,435 |
| M4 one word changed inside `about/` footer | `about/index.html:93  B-004 footer differs from index.html` | 1 | 6,342 |

**M3 and M4 are the pair that matters and both land.** M3 fired B-005 alone — B-004 did *not* also
report the footer, so §2's suppression is implemented. M4 still fired B-004 with the count at one, so
the suppression did not switch B-004 off. One defect, one violation, in both directions.

M2's banner rising to 69 internal links is correct: the duplicated block carries a second `/contact/`
href, and it resolves, so B-001 stays green. The implementer called this out unprompted.

The implementer's M1 mutated size (6,440) differs from the reviewer's (6,401) because the filler text
differs. Restored counts are identical. Not a finding.

## 4. Implementation — read, not inferred

`extractBlocks` walks `indexOf` from the end of the previous match; `extractBlock` keeps the
first-match behaviour B-004 already had. `checkChromeCount` runs before the B-004 pass and hands it a
per-page, per-block `ok` map. Imports are `node:fs`, `node:path`, `node:url` and nothing else. Pages
are still discovered from the filesystem. Banner first, verdict last, clean tree exits 0.

## 5. Two findings from mutations the contract did not ask for

**R1 — a defect on the reference page reports six times.** Delete the footer from `index.html` and
the gate emits one B-005 for `index.html` plus five `B-004 footer missing` lines, because
`chromeOk` is consulted only for `loaded.slice(1)`. It goes red loudly, which is the job, but it
reports one defect as six. The implementer disclosed this as residual 3. **Accepted as disclosed.**

**R2 — an unclosed second block is still invisible.** Append `<footer>` with no `</footer>` before
`</body>` and the gate prints `site check ok`, exit 0. The count is of complete start-plus-end pairs,
which is the same extraction B-004 uses and is exactly what §2 instructed. So this is correct against
the contract — and it is the same shape as the defect that shipped on `contact/index.html`: an
unterminated tag that renders fine. The implementer disclosed it as residual 1.

**Neither blocks approval. Both become B-006.**

## 6. What the implementer got right that is worth recording

It ran the gate and pasted the output. It restored from a captured byte array rather than from git,
which is the stronger proof. It flagged §5's M3 predicted-green column as narrower than reality
(B-001 stays green because a deleted footer has no `href`) instead of quietly matching the table. It
ran no `git` command. Third contract in a row where the push-back was worth reading.

## 7. Next

- Owner commits on a branch and merges with `--no-ff`. **No agent merges.**
- B-006 opens for the two residuals: collapse a reference-page chrome failure to one violation, and
  detect an unterminated chrome start tag. Contract to be written as `LWS-P1A-003`.
- `STATE.md` byte-count baseline is unchanged. Only the `check_site.mjs` line moves, to 9,413.
